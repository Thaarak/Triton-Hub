# Canvas OAuth (required for "Log in with Canvas")
# Get a Developer Key from your Canvas admin (e.g. UCSD); add the redirect URI below to the key.

CANVAS_BASE_URL=https://canvas.ucsd.edu
OAUTH_REDIRECT_URI=http://localhost:5173/oauth/callback

# OAuth server port (default 3001)
# PORT=3001
